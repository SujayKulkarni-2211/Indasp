import React from "react";
import "./EventSchedule.css";

const EventSchedule: React.FC = () => {
  return (
    <section className="schedule">
      <h2>Event Schedule</h2>
      <p>Details about session timings...</p>
    </section>
  );
};

export default EventSchedule;
